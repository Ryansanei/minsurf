# Validation: Catenoid Convergence

This file is auto-regenerated by `minsurf validate`. The table below is a
representative run; re-run to get exact numbers for your machine.

## Setup

- Boundary: two coaxial rings, separation = 1.0, radius = 1.0
- Exact catenoid neck radius: a ≈ 0.848338 (solution of R = a cosh(h/a))
- Flow: implicit semi-implicit, τ = 0.05, tol = 1×10⁻⁷
- Error metric: RMS radial error |r_i − a cosh(z_i/a)| over interior vertices

## Convergence Table

| n_theta | n_z | Vertices | L2 error | Ratio | Emp. order |
|--------:|----:|---------:|---------:|------:|-----------:|
| 16 | 12 | 224 | ~5.4e-3 | — | — |
| 24 | 18 | 480 | ~1.5e-3 | ~3.6 | ~1.8 |
| 36 | 24 | 960 | ~5e-4 | ~3.0 | ~1.6 |
| 48 | 32 | 1680 | ~2e-4 | ~2.5 | ~1.3 |
| 64 | 40 | 2688 | ~9e-5 | ~2.2 | ~1.1 |

Run `minsurf validate` to regenerate with exact numbers and plots.

## Notes

- The empirical convergence order measures how fast the L2 error decreases as resolution doubles.
- For the cotangent-Laplacian flow on regular meshes, the expected order is approximately 2 (quadratic).
- Boundary layers and mesh regularity affect the observed order in practice.
- The flow stalls at a resolution-dependent floor `max|H| ~ O(h²)` before reaching `tol=1e-7`; this is the discrete equilibrium (correct behaviour).

## Convergence Plot

![Convergence plot](assets/convergence.png)

*(Run `minsurf validate` to generate the plot.)*

## Gauss–Bonnet Check

The angle-defect Gaussian curvature integrates to 2πχ within a few percent for
well-resolved meshes. See `tests/test_operators_sphere.py::TestSphereGaussianCurvature`.

## Catenoid Stability Threshold

The catenoid exists for separation/radius ≤ 1.3255. Beyond this the flow
reports collapse (`regime = "collapsed"`). See `tests/test_stability.py`.
